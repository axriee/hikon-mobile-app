# Copy this file to .env (or follow platform-specific env guidance) and fill the values
# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# SMS endpoint (OPTION A: run the local server provided in server/send-sms.js and set SMS_API_URL to http://localhost:3000/send)
SMS_API_URL=http://localhost:3000/send

# Alternatively, if you run a server or Edge Function, put its URL here.

# Twilio (if you run the example server)
TWILIO_ACCOUNT_SID=your_twilio_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_FROM=+1234567890
